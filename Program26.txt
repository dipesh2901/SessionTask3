SELECT*,(SAL*1.1) AS INCRE_SAL FROM EMP AS E INNER JOIN DEPT AS D USING (DEPTNO) WHERE LOC='BOSTON';
SELECT COUNT(*) AS EMPLOYEES FROM EMP INNER JOIN DEPT USING (DEPTNO) WHERE DNAME='RESEARCH'; 
SELECT MAX(SAL) FROM EMP INNER JOIN DEPT USING (DEPTNO) WHERE DNAME='SALES';
SELECT ENAME,HIREDATE FROM EMP LEFT JOIN DEPT USING (DEPTNO) WHERE LOC IS NULL;